api_version = "2025-10"

[[extensions]]
type = "ui_extension"
name = "Store credit"
handle = "customer-credit"

  [[extensions.targeting]]
  # Full credit history page in the customer account.
  target = "customer-account.page.render"
  module = "./src/CreditPage.jsx"

  [[extensions.targeting]]
  # Compact balance block on the order index page.
  target = "customer-account.order-index.block.render"
  module = "./src/CreditBlock.jsx"

  [extensions.capabilities]
  # Required to read the balance from the CreditLoop backend. The extension does
  # not use the Customer Account API directly, so `api_access` is not requested.
  network_access = true
