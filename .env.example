# --- Database (Neon PostgreSQL) ---------------------------------------------
# Pooled connection used by the app at runtime.
#
# Keep `pgbouncer=true&connection_limit=1`. Without them every serverless
# invocation opens its own connection, which is slow and exhausts the pool.
#
# The database region must match the function region in vercel.json — a
# dashboard load makes about a dozen queries, so a cross-continent hop costs
# seconds. See README, "Neon setup".
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/creditloop?sslmode=require&pgbouncer=true&connection_limit=1"
# Direct (non-pooled) connection used by Prisma Migrate.
DIRECT_DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/creditloop?sslmode=require"

# --- Shopify app -------------------------------------------------------------
SHOPIFY_API_KEY=""
SHOPIFY_API_SECRET=""
SHOPIFY_APP_URL="https://your-app.vercel.app"
SHOPIFY_SCOPES="read_orders,write_orders,read_returns,read_customers,read_store_credit_accounts,write_store_credit_account_transactions"
# Verify against the current stable Shopify API version before deploying.
SHOPIFY_API_VERSION="2025-10"

# --- Email (Resend) ----------------------------------------------------------
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Your Store <credit@yourstore.com>"

# --- Cron --------------------------------------------------------------------
# Shared secret for /api/cron/* endpoints. Generate with: openssl rand -hex 32
CRON_SECRET=""

# --- Encryption --------------------------------------------------------------
# 32-byte key (hex or base64) used to encrypt Shopify access tokens at rest.
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=""

# --- Development only --------------------------------------------------------
# Enables the demo data generator. Never set this in production.
CREDITLOOP_DEMO_MODE="false"
